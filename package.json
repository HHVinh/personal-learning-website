{
  "name": "personal-learning-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "db:push": "npx wrangler d1 execute --database learning-db --file=migrations/001_init.sql --remote",
    "db:local": "npx wrangler d1 execute --database learning-db --file=migrations/001_init.sql --local"
  },
  "dependencies": {
    "@cloudflare/d1": "^1.4.1",
    "next": "^14.2.5",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "typescript": "^5.0.0"
  }
}
